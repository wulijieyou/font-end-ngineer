promise:解决异步

同步：串行 简单，方便
异步：并发  性能高，体验好

1.Promise有用 --> 解除异步操作，写法上看起来像是同步操作，但是内部是执行的异步操作
2.Promise局限 --> 无法解决一些带逻辑的异步操作，例如城市三级联动 

Promise.all() 所有的都成功
Promise.race() 只要有一个完成

-----------------------------------------------------------------------------------------------------


generator --> 生成器   能暂停

yield 
1.可传参数
2.可回调

-----------------------------------------------------------------------------------------------------
async/await

async function () {
    let 结果 = await 异步操作 --> promise,generator,另一个async函数
}

